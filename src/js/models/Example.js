import Model from './Model.js';

/**
 * Example model.
 * Class name here should match Eloquent model's class name.
 * Properties should match Eloquent model's attributes.
 * Done.
 */
export default class Example extends Model {
    id;
    created_at;
    updated_at;
}