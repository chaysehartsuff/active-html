<?php


return [
    /*
    | List of models allowed to be accessed via the JS library.
    | Users will add their own models here after publishing the config.
    */
    'models' => [
        // \App\Models\UserImage::class,
    ],
    /**
     * List of applied permissions for each action.
     */
    'action_permissions' => [
       // \App\Models\UserImage::class => BaseAll::class
    ],

    'action_processors' => [
    ]
];